service one_gadget
{
        type = UNLISTED
        protocol = tcp
        socket_type = stream
        port = 30003
        wait = no
        disable = no
        user = user
        server = /bin/sh
        server_args = /home/user/start.sh
}
